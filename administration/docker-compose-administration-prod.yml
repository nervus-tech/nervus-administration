version: '3.8'
services:
  app:
    extends:
      file: docker-compose-administration-base.yml
      service: app
    container_name: administration-prod
    ports:
      - "8110:8080"
    environment:
      - HIBERNATE_DEFAULT_SCHEMA=prod_admins
      - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka:8761/eureka/
      - SPRING_JPA_SHOW_SQL=false
    deploy:
      resources:
        limits:
          cpus: "1.5"
          memory: 3G
    networks:
      - nervus-network

networks:
  nervus-network:
    external: true